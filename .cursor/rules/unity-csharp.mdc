---
description: Padrões C# e Unity para scripts do Braziliation
globs: Assets/**/*.cs
alwaysApply: false
---

# Scripts C# / Unity – Braziliation

## Namespace
- Use `Braziliation` para o jogo. Subnamespaces por domínio: `Braziliation.Core`, `Braziliation.Systems`, etc.
- Exemplo: `namespace Braziliation.Core { ... }`

## Unity
- Preferir **MonoBehaviour** quando precisar de ciclo de vida (Start, Update, colisões).
- Para inicialização global (antes da cena): `[RuntimeInitializeOnLoadMethod(RuntimeInitializeLoadType.BeforeSceneLoad)]`.
- 2D: usar `UnityEngine` e `UnityEngine.Rendering.Universal` quando for URP 2D; física com `Rigidbody2D`/`Collider2D`.

## Resolução e câmera
- Projeto usa **Pixel Perfect**: ref 320×180, 16 PPU (definido em `CameraScaler`). Manter consistência em arte e tiles.

## Exemplo de inicializador
```csharp
[RuntimeInitializeOnLoadMethod(RuntimeInitializeLoadType.BeforeSceneLoad)]
static void Initialize()
{
    Application.targetFrameRate = 60;
    QualitySettings.vSyncCount = 0;
}
```

## Boas práticas
- Um script por responsabilidade; nomes claros (ex: `GameInitializer`, `CameraScaler`).
- Evitar lógica pesada em `Update`; usar eventos ou corrotinas quando fizer sentido.
- Documentar comportamento público e parâmetros de inspetor quando não forem óbvios.
